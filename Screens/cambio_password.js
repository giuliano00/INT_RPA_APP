import React from "react";
import { View, Text, StyleSheet, TouchableOpacity } from "react-native";

const cambio_password = () => {
    return (
        <View>
            <Text
                style={{
                    fontSize: 30,
                    textAlign: "center",
                    marginTop: "20%"
                }}
            >cambio_password</Text>
        </View>
    );
    }

export default cambio_password;