import React from "react";
import { View, Text, StyleSheet, TouchableOpacity } from "react-native";

const edicion_alarma = () => {
    return (
        <View>
            <Text
                style={{
                    fontSize: 30,
                    textAlign: "center",
                    marginTop: "20%"
                }}
            >edicion_alarma</Text>
        </View>
    );
    }

export default edicion_alarma;